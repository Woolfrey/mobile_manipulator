#include "ros/ros.h"
#include "MobileManipulator.h"

int main(int argc, char **argv)
{
	ros::init(argc, argv, "mobile_manipulator");
	ros::NodeHandle n;

	while(ros::ok())
	{

	}
	
	return 0;					// No problems with main()
}
